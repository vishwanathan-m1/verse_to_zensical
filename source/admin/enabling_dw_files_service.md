{% if isDominoWorkspace %}
 # Enabling Domino Workspace files service

   To make this feature available to your users, administrators must configure the integration between the mail environment and the Domino Workspace Files service.  
   This feature is disabled by default and can be enabled by applying the notes.ini setting VOP_GK_FEATURE_298=1 on the Domino server that runs HCL Domino Workspace.  
{% endif %} 